[pytest]
# importlib mode lets the suite run agents + platform + governance together
# despite each having an identically-named `tests/` package.
addopts = --import-mode=importlib
python_files = test_*.py
python_classes = Test*
python_functions = test_*
